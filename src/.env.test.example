# Database - local Supabase Postgres direct (default below) or docker-compose.db.yml on :5435/crm_test
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54324/postgres"

# Auth.js - use when running Next.js against DATABASE_URL above
AUTH_SECRET="test-auth-secret-change-in-ci-64chars____________"
AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Redis not required for db:test:setup; BullMQ/workers optional for E2E
REDIS_URL="redis://127.0.0.1:6381"
